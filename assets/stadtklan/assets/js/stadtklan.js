/**
 * Created by michaelrutz on 14.09.14.
 */

$(document).ready(function(){
    /*$('.home-box').click(
        function(){
            var $this = $(this),
                $btn = $this.find('.more-btn'),
                $collapsible = $this.find('.collapse');

            if ($collapsible.is(':visible')) {
                $btn.html('mehr &raquo;');
                $collapsible.collapse('hide');
            } else {
                $btn.html('&laquo; weniger');
                $collapsible.collapse('show');
            }
        }
    );*/

    // add an ical download to every event
    /*$('.h-event').Icalize({
        placement : 'before',
        template : '<button class="btn btn-default btn-sm"><span class="glyphicon glyphicon-download"></span></button>'
    }); */


});